// ============================================================
// 时光机回放 — 全局状态（跨组件共享）
// ============================================================
import { ref } from 'vue'

/** 是否处于回放模式中（供急停按钮禁用判断） */
export const isReplaying = ref(false)
